# Note we depend on NODE_ENV being set to dictate
# which of the env variables below get loaded at runtime.
# See README for more details.

# Application Port - express server listens on this port (default 3000).
PORT=3000
REDIS_URI='redis://localhost'
SLACK_API_TOKEN=
JIRA_USERNAME=
JIRA_API_TOKEN=
